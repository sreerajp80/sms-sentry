# Downgrade AGP 9.1.1 → 8.13.0

Implements [plans/20260613_100221_agp-downgrade.md](../plans/20260613_100221_agp-downgrade.md).

## What changed

- **gradle/libs.versions.toml** — `agp = "9.1.1"` → `agp = "8.13.0"` to match the
  IDE's supported tooling (latest supported AGP is 8.13.0).

## Verification

- Grep confirmed no residual AGP 9.x pin; the only other `agp` reference is the
  `version.ref = "agp"` in the `android-application` plugin alias, which now resolves
  to 8.13.0.
- Gradle wrapper (9.3.1), Kotlin (2.2.10), and KSP (2.3.5) left unchanged — all
  compatible with AGP 8.13.0.
- A full Gradle sync/build was not run here (no confirmed Gradle environment in this
  session); recommend re-syncing in the IDE to confirm the error clears.
